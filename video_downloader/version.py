# Единственное место, где живёт номер версии.
# Его читают: страница (через Api.bootstrap), проверка обновлений
# и сборка установщика (build.bat передаёт его в Inno Setup через /DAppVersion).
APP_VERSION = "0.1.0"
